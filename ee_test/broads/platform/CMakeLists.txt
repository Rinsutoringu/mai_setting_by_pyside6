file(GLOB_RECURSE PLATFORM_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp)

add_board_specific_library(${BOARD_NAME}_platform
		TARGET ${BOARD_NAME}
		SOURCES ${PLATFORM_SOURCES}
		INCLUDES ${CMAKE_CURRENT_SOURCE_DIR}/include
)
